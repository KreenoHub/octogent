# Todo

- [x] **Session runner** — in `apps/octoplan/server/bridge/sessionRunner.ts`, create a `createSessionRunner({ query, store, getMode })` factory. `start(repoPath, mode, topic)` runs one Agent SDK `query()` per session in streaming-input mode with `cwd = repoPath` and `systemPrompt: { type: "preset", preset: "claude_code", append: mode.systemPromptAppend }`. It records `claudeSessionId` from the `system`/`init` message. It maps assistant text to `section` MessageBlocks, splitting at markdown headings, and `tool_use` to `tool` blocks. It exposes `send(text)` to push a user turn, and `stop()` via AbortController. Wire `start-session` / `send-message` / `stop-session` in `server/createServer.ts` and broadcast `session-updated` + `block` events. Done when `tests/bridge/sessionRunner.test.ts` feeds a scripted SDKMessage stream (no real SDK) and asserts the emitted events in order, and `pnpm --filter @octogent/octoplan test` passes.
- [x] **AskUserQuestion interception** — `canUseTool` in `server/bridge/askUserQuestion.ts`: For `AskUserQuestion` it assigns `Q<n>` ids, emits a `question-round`, sets the session to `waiting-for-answer`, and awaits the matching `answer-round`. It then resolves `{ behavior: "allow", updatedInput: { ...input, answers: encodeAnswersForTool(questions, answers) } }`. It hands the answers to `store.recordAnswers(...)`. The store also writes PARKED/RISKS records for parked/tentative answers. Any other tool not in the mode's allowlist is denied with a "planning session" message. The abort signal and `stop-session` reject a pending round cleanly. Keep the PreToolUse-hook fallback out of scope; only leave a note in the file header. Done when tests cover single-select, multi-select (comma-joined), Other text, parked, tentative, deny-list and abort-while-waiting, all with a fake `query`.
- [x] **Plan tools MCP server** — in `server/bridge/planTools.ts`, build `createSdkMcpServer({ name: "octoplan", tools })` with zod 4 input schemas for `plan_record_decision`, `plan_update_coverage`, `plan_add_gap`, `plan_add_risk`, `plan_park`, `plan_add_idea` and `plan_write_goal`. Each handler calls the matching `PlanStore` method and returns a short text confirmation containing the record id (e.g. "Recorded D4"). After each handler, broadcast a `plan` event with the store's fresh snapshot. Done when tests invoke each tool handler directly against a fake PlanStore and assert the store calls and the confirmation text, and the tools are passed as `mcpServers.octoplan` by the session runner.
- [x] **Revision flow** — on `revise-answer`: Look up the previous answer and find the decisions whose `questions`/`depends-on` include that question id via `store.dependentDecisions(questionId)`. Mark them `stale` through the store, then push `encodeRevisionTurn(...)` as a user turn into the live session. Emit `plan` so the UI shows the stale badges immediately. If the session has ended, resume it (`options.resume = claudeSessionId`) before pushing the turn. Done when a test revises Q1, asserts D2 turns stale, and asserts the exact REVISION text reached the fake query's input stream.

## Wave 2

- [ ] **Pop-out terminal** — `server/bridge/popOut.ts` opens `claude --resume <claudeSessionId>` in a node-pty session in the session's repo cwd and streams it over a dedicated WS channel (`/ws/terminal/<sessionId>`). Reuse the pattern in `apps/api/src/terminalRuntime/` by reading it, not importing it. Done when a test with a fake pty factory asserts the spawn command/cwd and that data flows both ways, and a manual run opens a working terminal.
- [ ] **Fork session for conversation branching** — `branch(sessionId, fromBlockId, title)` starts a new session with `options.resume = claudeSessionId` + `forkSession: true`. It records a `ConversationBranch` via the store (`branches.md`) and broadcasts both sessions. Done when a test asserts the fork options passed to `query` and the B-record written through the fake store.
